#### 20. 리액트에서 웹 폰트를 적용하는 방법이 아닌 것은 무엇인가요?

1. \<link> 태그를 사용해 구글 폰트에서 불러온다.
2. @import를 사용해 CSS 파일에서 웹 폰트를 불러온다.
3. @font-face를 사용해 웹 서버에 있는 폰트 파일을 직접 불러온다.
4. 웹 브라우저에서 자동으로 사용자의 로컬 폰트를 인식해 웹 폰트로 적용한다.

> 4
